// Crie criarMultiplicador(fator) que retorna uma função
// A função retornada multiplica qualquer número pelo fator

deixe duplicar = criarMultiplicador(2)
deixe triplicar = criarMultiplicador(3)

imprima(duplicar(4))
imprima(triplicar(4))
